<div id="i_wonder_wrapper" class="show">
	<h1><%= @metric.name%> </h1>
	
	<p>
		<%= link_to "< Back to index", metrics_path %>
		|
		<%= link_to "Edit", edit_metric_path(@metric) %>
		|
		<%= link_to "Delete", metric_path(@metric), :method => :delete, :confirm => "Are you sure?" %>
	</p>
</div>